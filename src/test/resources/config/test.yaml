server:
  type: default
  rootPath: /api/

representativeClient:
  baseUri: http://whoismyrepresentative.com/

httpClient: {}

twilio:
  accountSid: FAKE_ACCOUNT_SID
  authToken: FAKE_AUTH_TOKEN
  phoneNumbers:
    - 1-800-CAL-LATT

authentication:
  credentials:
    twilio: FAKE_PASSWORD

service:
  baseUri: http://localhost

redis:
  url: redis://localhost:6379

googleMaps:
  apiKey: FAKE_KEY
